# Web 框架
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9

# 数据验证
pydantic>=2.7.0
pydantic-settings>=2.2.0

# 安全（JWT 校验，消费 user-service 令牌）
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0

# HTTP 客户端（拉取 user-service JWKS / 调用 meta-service）
httpx>=0.27.0

# 说明
# - 无数据库存储：不引 SQLAlchemy / aiosqlite / alembic（复用 Meta Storage）
# - 不引 Redis（v1 用进程内 per-user 锁；升级路径见 PLAN.md §5.5）
# - 本服务不签发令牌、不存用户表，无需 passlib/bcrypt
# - 生产使用纯 uvicorn；热重载与测试依赖见 requirements-dev.txt
